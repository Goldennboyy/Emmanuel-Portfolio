/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
};

// module.exports = {
//   alias: {
//     mymodule: path.resolve(__dirname, "./lib/interface.d"),
//   },
// };

module.exports = nextConfig;
